# 🧪 Инструкции по тестированию ZIGGURAT MIND

## 🚀 Быстрый запуск всех тестов

### Windows
```cmd
test_all_functions.bat
```

### Linux/macOS
```bash
chmod +x test_all_functions.sh
./test_all_functions.sh
```

## 📊 Запуск отдельных категорий тестов

### Только компиляция
```bash
# Windows
test_all_functions.bat compile_only

# Linux/macOS  
./test_all_functions.sh compile_only
```

### Только тесты с памятью
```bash
# Windows
test_all_functions.bat memory_tests_only

# Linux/macOS
./test_all_functions.sh memory_tests_only
```

### Только демонстрация
```bash
# Windows
test_all_functions.bat demo_only

# Linux/macOS
./test_all_functions.sh demo_only
```

---

## 🔍 Краевые тесты
```bash
# Проверка обработки пустых и некорректных данных
test_all_functions.bat edge_cases_only
```

---

## 📊 Производительность
```bash
# Нагрузочное тестирование системы памяти
test_all_functions.bat performance_stress
```

---

## 🎭 Демонстрация возможностей

### Научный ассистент
```bash
test_all_functions.bat demo_scholar
```

### Рассказчик
```bash
test_all_functions.bat demo_storyteller
```

### Философ
```bash
test_all_functions.bat demo_philosopher
```

---

## 📋 Анализ результатов

После выполнения тестов система автоматически генерирует отчет с:
- ✅/❌ статусом каждого теста
- Детальной статистикой
- Рекомендациями по улучшению

---

## 🛠️ Требования

### Обязательные:
1. **Модель эмбеддингов** в `models/embeddings/`
   - `intfloat/multilingual-e5-small` (118MB, 384 dims)
   - Содержит: `config.json`, `model.safetensors`, `tokenizer.json`

2. **Rust компилятор** с поддержкой `async`
3. **Базовые утилиты**: `cargo`, `git`

### Рекомендуемые:
- **8GB+ RAM** для комфортной работы с памятью
- **GPU** для ускорения эмбеддингов (опционально)
- **SSD** для быстрых операций с памятью

---

## 🚀 Начните тестирование!

**1. Запустите полную проверку:**
```bash
test_all_functions.bat
```

**2. Следите за выводом и ждите завершения**
   - Каждый тест показывает свой статус
   - Финальный отчет появится автоматически

**3. Если发现问题 - используйте рекомендации из отчета**

---

*После тестирования вы можете использовать систему в обычном режиме:* 🚀

```bash
cargo run --bin ziggurat-enhanced --enable-memory --persona scholar
```