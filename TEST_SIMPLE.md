# 🧪 Простой тестовый набор для ZIGGURAT MIND

## 📋 Инструкции

### 🚀 Запуск
1. Запустите `test_simple.bat` для базовой проверки
2. Следите выводу и ждете завершения
3. Все результаты сохраняются в `test_results/`

## 📝 Базовые тесты (без памяти)
```bash
# 1. Компиляция
echo "Подтест 1: Компиляция..."
cargo check --bin ziggurat-ind --release --quiet

# 2. Базовый диалог
echo "Подтест 2: Базовый диалог..."
cargo run --bin ziggurat-ind --release --prompt "Привет!" --cpu --sample-len 50

# 3. Эмбеддинги
echo "Подтест 3: Эмбеддинги..."
if [ -d "models/embeddings" ]; then
    echo "⚠  ✅ Модель эмбеддингов найдена"
else
    echo "⚠️  ❌ Модель эмбеддингов не найдена"
fi

# 4. Простой промпт
echo "Подтест 4: Простой промпт..."
cargo run --bin ziggurat-ind --release --prompt "Расскажи что ты умеешь" --cpu --temperature 0.0 --top_k 40 --sample-len 100

# 5. Большой промпт
echo "Подтест 5: Большой промпт..."
cargo run --bin ziggurat-ind --release --prompt "Напиши подробную историю развития квантовых вычислений, начиная с ранних экспериментов и заканчивая современными концепциями. Включи математические формулы, ключевые эксперименты и их интерпретацию." --cpu --temperature 0.8 --top_k 50 --sample-len 200

# 6. Тест производительности
echo "Подтест 6: Производительность..."
cargo run --bin ziggurat-ind --release --prompt "Покажи скорость генерации токенов в секунду" --cpu --sample-len 500 --seed 42

# Результаты
echo "📊 Сохраняем результаты..."
```

---

## 📦 Расширенные тесты (с памятью)

### 🧠 Функции памяти
```bash
# 1. Извлечение концептов
echo "Подтест 1: Извлечение концептов..."
cargo run --bin ziggurat-enhanced --release --prompt "Что такое нейронная сеть?" --enable-memory --memory-concepts-count 5

# 2. Семантический поиск
echo "Подтест 2: Семантический поиск..."
cargo run --bin ziggurat-enhanced --release --prompt "Расскажи о машинном обучении" --enable-memory --memory-episodes-count 0 --memory-concepts-count 5

# 3. Управление сессиями
echo "Подтест 3: Управление сессиями..."
cargo run --bin ziggurat-enhanced --release --prompt "Начни новую сессию с именем 'TestSession'" --enable-memory

# 4. Персистентность
echo "Подтест 4: Персистентность..."
cargo run --bin ziggurat-enhanced --release --export-memory test_backup.json

# 5. Импорт памяти
echo "Подтест 5: Импорт памяти..."
cargo run --bin ziggurat-enhanced --release --import-memory test_backup.json --enable-memory

# 6. Статистика
echo "Подтест 6: Статистика..."
cargo run --bin ziggurat-enhanced --release --memory-stats

# 7. Очистка старых данных
echo "Подтест 7: Очистка старых данных..."
cargo run --bin ziggurat-enhanced --release --cleanup-days 7 --memory-stats

# 8. Краевые случаи
echo "Подтест 8: Краевые случаи..."
cargo run --bin ziggurat-enhanced --release --prompt "" --enable-memory

# 9. Научные ассистенты
echo "Подтест 9: Научные ассистенты..."
cargo run --bin ziggurat-enhanced --release --prompt "Я научный ассистент. Проанализируй тему 'дата наука' с использованием всех доступных данных." --persona researcher --enable-memory --memory-concepts-count 5 --memory-episodes-count 10

# 10. Интегральная демонстрация
echo "Подтест 10: Интегральная демонстрация..."
cargo run --bin ziggurat-enhanced --release --prompt "Привет! Я готов показать тебе все возможности моей системы памяти." --enable-memory --memory-episodes-count 3 --memory-concepts-count 3 --persona companion --temperature 0.7

---

## 📈 Автоматические тесты

### 🔄 Стресс-тестирование
```bash
# Тестирование надежности системы
echo "Подтест 11: Стресс-тестирование..."
# Создаем стрессовую нагрузку
for i in {1..100}; do
    cargo run --bin ziggurat-enhanced --release --prompt "Стресс-тест $i/100: Сгенеруй ответ на 100 токенов и сохрани его в память." --enable-memory --memory-episodes-count 1 --sample-len 100
done

# Анализ производительности
echo "Подтест 12: Анализ производительности..."
cargo run --bin ziggurat-enhanced --release --memory-stats

## 🔍 Интеграционные тесты

### 🎯 Проверка совместимости
```bash
# Проверяем что все компоненты работают вместе
echo "Подтест 13: Интеграционная проверка..."
cargo run --bin ziggurat-enhanced --release --prompt "Интеграция теста..." --enable-memory --memory-episodes-count 3 --memory-concepts-count 2
```

---

## 📝 Отчет о результатах

После завершения всех тестов будет сгенерирован детальный отчет в `test_results/` с рекомендациями по улучшению.

---

## 🚀 Начните с простого режима!
echo "🚀 Запускаем простую версию..."
cargo run --bin ziggurat-ind --release

---

## 📋 **Что делает тестовый набор:**

1. **Проверяет все компоненты** по отдельности
2. **Анализирует производительность** и использование ресурсов
3. **Тестирует краевые случаи** и обработку ошибок
4. **Демонстрирует интеллектуальные возможности** с реальными данными
5. **Генерирует отчеты** с рекомендациями

**Используйте его для проверки стабильности перед добавлением новых функций!**

---

## 🔧 **Запуск:**
```bash
cd /path/to/ziggurat-mind
test_all_functions.bat
```

**Windows:**
```cmd
/C/path/to/ziggurat-mind/test_all_functions.bat
```

**Пример использования:**
```bash
# Только компиляция
test_all_functions.bat compile_only

# Только тесты памяти
test_all_functions.bat memory_tests_only

# Только демонстрация
test_all_functions.bat demo_scholar

# Производительность
test_all_functions.bat performance_stress
```

---

**Примечание:**
- Для работы с памятью требуется модель эмбеддингов в `models/embeddings/`
- Система автоматически проверяет доступность модели
- Результаты сохраняются в `test_results/` для анализа

**Удачи тестирования!** 🎯