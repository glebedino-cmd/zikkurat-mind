# 🏛️ CORE PHILOSOPHY

## ZIGGURAT MIND - Философия Проекта

### Фундаментальные Принципы

**1. Архитектура Сознания**
ZIGGURAT MIND строится как многоуровневая система, вдохновленная структурой месопотамского зиккурата - каждый уровень добавляет новую способность:

- **🜂 Уровень 1: Жрецы (Priests)** - Инфраструктура
  - Embedding Engine (e5-small, 384-мерные векторы)
  - Device Management (GPU CUDA / CPU fallback)
  - Resource Allocation и оптимизация памяти

- **🜃 Уровень 2: Тотемы (Totems)** - Память
  - Эпизодическая память (диалоги, сессии)
  - Семантическая память (концепты, факты, предпочтения)
  - Векторное хранилище (cosine similarity поиск)

- **🜄 Уровень 3: Логос (Logos)** - Рассуждение
  - Mistral 7B Inference (локальная модель)
  - Tokenization & Sampling Strategies
  - Контекстное понимание с памятью

**2. Принцип Единой Точки Входа**
Все запросы проходят через Unified Pipeline:
```
Query → Embed → Search → Context → Generate → Save
```

**3. Типы Памяти**

### Эпизодическая Память
Система запоминает контекст разговора через:
- Векторизацию каждого обмена (user + assistant) → 384-мерный вектор
- Cosine similarity для поиска похожих диалогов
- Персистентность: текст в JSON, эмбеддинги в бинарном файле
- Восстановление состояния при перезапуске

### Семантическая Память
Хранит структурированные знания в виде концептов:
- **Facts** - факты о пользователе или мире
- **Rules** - правила и инструкции
- **Preferences** - предпочтения и вкусы
- **Skills** - навыки и способности
- **General** - общие знания

Каждый концепт имеет:
- Уникальный UUID
- Текст утверждения
- Категорию (facts/rules/preferences/skills/general)
- Уверенность (confidence: 0.0-1.0)
- Источник (session_id или "manual")
- Метаданные
- Счетчик использований (usage_count)
- Автоматическую векторизацию

**Голосование за концепты:**
Пользователь может повышать или понижать уверенность концепта:
- `/semantic vote <id> up` - увеличить уверенность на 0.1
- `/semantic vote <id> down` - уменьшить уверенность на 0.1

**4. Автоматическое Извлечение Концептов**
После каждого ответа система автоматически извлекает знания из диалога:
```
Dialogue → LLM Extraction (Mistral 7B) → JSON Validation → Vectorization → Storage
```

Извлечение использует оптимизированный промпт в формате Mistral Instruct:
- Ограничение длины концепта: 100 символов
- Улучшенный парсинг JSON с fallback-механизмами
- Категоризация: facts, rules, preferences, skills, general
- Оценка уверенности: 0.0-1.0

**5. Мониторинг Ресурсов**
Система отслеживает использование памяти:
- RAM: чтение из /proc/self/status (Linux)
- VRAM: запрос через nvidia-smi (при наличии GPU)
- Команда `/mem` для отображения текущего потребления

**5. Непрерывное Обучение**
Каждый диалог сохраняется и используется для будущих контекстов. Система учится на каждом взаимодействии.

### Технические Принципы

- **Rust + Candle** - Безопасность и производительность
- **CUDA Optimization** - Максимальная скорость инференса (RTX 4090: ~45 tok/s)
- **Memory Optimization** - Одна модель, автоочистка старых сессий
- **Semantic Search** - Поиск по смыслу, не по ключевым словам
- **Binary Persistence** - Эффективное хранение эмбеддингов

### Формат Хранения

```
memory_data/
├── sessions.json          # Эпизодическая память (JSON)
├── semantic_memory.json   # Семантическая память (JSON)
├── embeddings.bin         # Векторы (binary format)
└── metadata.json          # Версия, размерность, статистика
```

**embeddings.bin формат:**
```
[Header: 32 bytes]
- version: u32
- embedding_dim: u32
- num_embeddings: u64
- index_offset: u64
- data_offset: u64

[Index entries: 32 bytes each]
- session_id: 16 bytes (UUID)
- turn_idx: u32
- offset: u64
- size: u32

[Embedding data: 4 bytes per float]
- f32 vectors (384 dimensions)
```

**semantic_memory.json формат:**
```json
{
  "version": "1.0",
  "created_at": "2026-01-20T...",
  "last_saved_at": "2026-01-20T...",
  "total_concepts": 5,
  "concepts": [
    {
      "id": "uuid",
      "text": "User prefers dark mode",
      "category": "preferences",
      "confidence": 0.9,
      "source": "session-uuid",
      "metadata": {},
      "created_at": "2026-01-20T...",
      "updated_at": "2026-01-20T...",
      "usage_count": 0
    }
  ]
}
```

### Текущие Возможности

| Функция | Статус | Описание |
|---------|--------|----------|
| Эмбеддинги e5-small | ✅ | 384-мерные векторы |
| Векторное хранилище | ✅ | In-memory cosine similarity |
| Эпизодическая память | ✅ | Диалоги с авто-векторизацией |
| Семантическая память | ✅ | Концепты (facts/rules/preferences/skills) |
| Персистентность | ✅ | JSON + Binary embeddings |
| Mistral 7B | ✅ | Локальная модель (CUDA: BF16) |
| Поиск по памяти | ✅ | Semantic similarity |
| Auto-extraction | ✅ | Авто-извлечение концептов из диалогов |
| CRUD команды | ✅ | /semantic add/list/search/delete/get/vote |
| Мониторинг памяти | ✅ | /mem для RAM/VRAM |

### Интерактивные Команды

```bash
# Семантическая память
/semantic help                          # Показать справку
/semantic list [n]                      # Список концептов (по умолчанию 10)
/semantic list <category> [n]           # Список по категории (facts/rules/preferences/skills/general)
/semantic stats                         # Статистика памяти
/semantic search <query>                # Поиск концептов
/semantic add "<text>" <category> [c]   # Добавить концепт
/semantic vote <id> <up|down>           # Изменить уверенность концепта
/semantic delete <id>                   # Удалить концепт
/semantic get <id>                      # Показать концепт по ID
/semantic merge                         # Объединить дубликаты

# Мониторинг
/mem                                   # Показать использование RAM/VRAM

# Сокращения
/s help
/s list
/s list facts
/s vote <id> up
/s stats
/s search pizza
/s add "User loves pizza" preferences 0.9
```

### Примеры Использования

```bash
# Запуск с CUDA (RTX 4090)
cargo run --bin ziggurat-unified --features cuda --release -- \
  --enable-memory \
  --enable-semantic \
  --interactive

# Добавление концепта
/semantic add "Пользователь любит Python" preferences 0.9

# Список по категории
/semantic list preferences
/semantic list facts 20

# Поиск по смыслу
/semantic search "программирование"

# Голосование за концепт
/semantic vote <uuid> up
/semantic vote <uuid> down

# Проверка памяти
/semantic stats

# Мониторинг ресурсов
/mem
```

### Оптимизация Памяти

**На GPU (CUDA):**
- Mistral 7B использует VRAM (~4-6GB BF16)
- System RAM: минимальное использование
- Скорость: 40-50 токенов/секунду
- Мониторинг VRAM через nvidia-smi

**На CPU:**
- Mistral 7B требует ~15-20GB RAM
- Автоочистка старых сессий (лимит: 50)
- Рекомендуется использовать GPU

**Проверка ресурсов:**
```bash
/mem  # Показывает RAM и VRAM (если доступно)
```

### Путь Развития

```
MVP → Эпизодическая память → Семантическая память → AGI
  │              │                    │              │
  ▼              ▼                    ▼              ▼
Базовая     Полная память      Извлечение      Сознание
архитектура  диалогов          концептов       и обучение
             + автопоиск      + автоизвлечение + обучение
```

---

# 📖 ПОЛНОЕ РУКОВОДСТВО ПО СЕМАНТИЧЕСКОЙ ПАМЯТИ

## Что Такое Семантическая Память?

Семантическая память — это структурированное хранилище знаний ZIGGURAT MIND. В отличие от эпизодической памяти, которая запоминает конкретные диалоги, семантическая память извлекает **абстрактные знания** из диалогов и хранит их в виде концептов.

### Зачем Нужна Семантическая Память?

**Проблема эпизодической памяти:**
```
Диалог: "Я люблю Python для машинного обучения"
→ Сохраняется как есть
→ При поиске "программирование" может не найтись
→ Не понимает, что "машинное обучение" связано с AI
```

**Семантическая память решает это:**
```
Диалог → Извлечение → "User likes Python" (preferences, 0.9)
         → Векторизация → 384-мерный вектор
         → Поиск по смыслу: "программирование" → "Python" (сходство 0.85)
```

---

## Категории Концептов

Семантическая память использует 5 категорий для классификации знаний:

| Категория | Обозначение | Примеры | Влияние на систему |
|-----------|-------------|---------|-------------------|
| **Facts** | Факты | "Пользователь работает в IT", "Земля круглая" | Используются как базовые знания о мире и пользователе |
| **Rules** | Правила | "Не беспокоить после 22:00", "Говорить на ты" | Влияют на поведение системы и стиль общения |
| **Preferences** | Предпочтения | "Любит темную тему", "Предпочитает краткие ответы" | Адаптирует ответы под предпочтения пользователя |
| **Skills** | Навыки | "Пользователь знает Python", "Умеет готовить пасту" | Позволяет давать более релевантные советы |
| **General** | Общие знания | "Столица Франции — Париж" | Общий контекст и факты |

### Как Выбирается Категория?

При автоматическом извлечении LLM анализирует текст:
- **Факт**: утверждение о состоянии мира или пользователя
- **Правило**: инструкция, ограничение, предпочтение с "должен/нельзя"
- **Предпочтение**: "люблю/не люблю", "предпочитаю"
- **Навык**: "умею/знаю/могу"
- **Общее**: всё остальное полезное знание

---

## Атрибуты Концепта

Каждый концепт содержит:

### 1. UUID (Идентификатор)
```
b943783d-8549-4299-99e7-a85b45ec1db9
```
- Уникальный 128-битный идентификатор
- Используется для всех операций с концептом
- Нельзя изменить после создания

### 2. Text (Текст)
```
"User prefers dark mode for coding"
```
- Краткое утверждение (максимум ~100 символов)
- Извлекается из диалога или задаётся вручную
- Векторизуется для поиска по смыслу

### 3. Category (Категория)
```
preferences
```
- Одно из: facts, rules, preferences, skills, general
- Влияет на приоритет при поиске
- Группирует концепты для фильтрации

### 4. Confidence (Уверенность)
```
0.9
```
- От 0.0 до 1.0
- **0.0** — полная неуверенность, возможно ошибка
- **0.5** — нейтральная уверенность
- **1.0** — полная уверенность, проверенный факт

**Как влияет на систему:**
- Приоритет в поиске: концепты с высокой confidence показываются первыми
- Фильтрация: можно игнорировать концепты с низкой confidence
- Обучение: vote up/down изменяет confidence

### 5. Source (Источник)
```
session-20240120-143022
```
- `session-XXX` — автоматически извлечено из диалога
- `manual` — добавлено вручную через `/semantic add`
- Показывает происхождение знания

### 6. Usage Count (Счётчик Использований)
```
42
```
- Увеличивается каждый раз, когда концепт извлекается для ответа
- Помогает определить "важные" концепты
- Может использоваться для приоритизации

### 7. Timestamps (Временные Метки)
```
created_at: 2026-01-20T14:30:00Z
updated_at: 2026-01-20T14:30:00Z
```
- `created_at` — время создания (не изменяется)
- `updated_at` — время последнего изменения (vote, использование)
- Используется для сортировки в списках

---

## Подробное Описание Команд

### `/semantic help`

**Что делает:** Показывает справку по всем командам.

**Пример вывода:**
```
📚 Semantic Memory Commands:
  /semantic help           - Show this help
  /semantic list [n]       - List concepts (default: 10)
  /semantic list <category> [n] - List concepts by category
  /semantic stats          - Show statistics
  /semantic search <query> - Search concepts
  /semantic add "<text>" <category> [confidence] - Add new concept
  /semantic vote <id> <up|down> - Vote to adjust concept confidence
  /semantic delete <id>    - Delete concept by ID
  /semantic get <id>       - Show concept by ID
  /semantic merge          - Merge duplicate concepts
```

**Влияние на систему:** Никакого. Только информация.

---

### `/semantic list [n]`

**Что делает:** Показывает последние добавленные концепты.

**Параметры:**
- `n` — количество концептов (по умолчанию 10, максимум не ограничен)

**Примеры:**
```bash
/semantic list              # Показать 10 последних
/semantic list 20           # Показать 20
/semantic list 100          # Показать 100
```

**Пример вывода:**
```
📚 Concepts (showing 9 of 9):
1. [preferences] User likes Python (conf: 0.90)
2. [preferences] Dark mode preferred (conf: 0.85)
3. [facts] User works in IT (conf: 0.80)
...
```

**Сортировка:** По `updated_at` (самые свежие первыми)

**Влияние на систему:** Никакого. Только чтение.

---

### `/semantic list <category> [n]`

**Что делает:** Показывает концепты только указанной категории.

**Параметры:**
- `<category>` — обязательно: facts, rules, preferences, skills, general
- `[n]` — количество (по умолчанию 20)

**Примеры:**
```bash
/semantic list preferences       # Все предпочтения
/semantic list facts 10          # 10 последних фактов
/semantic list rules             # Все правила
/semantic list skills 5          # 5 навыков
```

**Пример вывода:**
```
📚 Concepts in preferences (showing 4 of 4):
1. [preferences] User likes Python (conf: 0.90)
2. [preferences] Dark mode preferred (conf: 0.85)
3. [preferences] Prefers VS Code (conf: 0.75)
4. [preferences] Likes coffee (conf: 0.60)
```

**Влияние на систему:** Никакого. Только чтение.

---

### `/semantic stats`

**Что делает:** Показывает статистику семантической памяти.

**Пример вывода:**
```
📚 Semantic Memory Stats:
   Total concepts: 9
   Average confidence: 0.78
   Total usage count: 156
   By category:
      - preferences: 4
      - facts: 2
      - skills: 2
      - rules: 1
      - general: 0
```

**Метрики:**
- `Total concepts` — общее количество концептов
- `Average confidence` — средняя уверенность всех концептов
- `Total usage count` — сколько раз концепты использовались
- `By category` — распределение по категориям

**Влияние на систему:** Никакого. Только чтение.

---

### `/semantic search <query>`

**Что делает:** Ищет концепты по смыслу (не по ключевым словам).

**Параметры:**
- `<query>` — поисковый запрос (любая строка)

**Примеры:**
```bash
/semantic search programming
/semantic search "dark theme"
/emantic search "what does user like"
```

**Как работает поиск:**
1. Запрос векторизуется через e5-small
2. Вычисляется cosine similarity со всеми концептами
3. Результаты сортируются по похожести
4. Возвращаются топ-10 концептов

**Пример вывода:**
```
Found 4 concepts:
1. [preferences 0.92] User likes Python - a1b2c3...
2. [skills 0.85] Python programming skill - d4e5f6...
3. [preferences 0.78] Dark mode preferred - g7h8i9...
4. [facts 0.72] User works with code - j0k1l2...
```

**Влияние на систему:**
- Концепты не изменяются
- Используется для просмотра релевантных знаний
- **Важно:** usage_count НЕ увеличивается при поиске

---

### `/semantic add "<text>" <category> [confidence]`

**Что делает:** Добавляет новый концепт вручную.

**Параметры:**
- `"<text>"` — текст концепта в кавычках
- `<category>` — facts, rules, preferences, skills, general
- `[confidence]` — уверенность 0.0-1.0 (по умолчанию 0.5)

**Примеры:**
```bash
/semantic add "Пользователь любит кофе" preferences 0.9
/semantic add "Говорить на Вы" rules 0.8
/semantic add "Пользователь из Москвы" facts 0.95
/semantic add "Можно задавать вопросы" rules
```

**Ограничения:**
- Текст не должен быть пустым
- Category должен быть одним из 5
- Confidence должен быть в диапазоне 0.0-1.0

**Пример вывода:**
```
✅ Added concept: a1b2c3d4-e5f6-7890-abcd-ef1234567890 (preferences)
```

**Влияние на систему:**
- ✅ Создаётся новый концепт
- ✅ Генерируется UUID
- ✅ Создаётся эмбеддинг (384-мерный вектор)
- ✅ Концепт добавляется в индекс категории
- ✅ Записывается в semantic_memory.json
- ❗ Увеличивает размер контекста при ответах

**Когда использовать:**
- Добавление знаний, которые система не извлекла автоматически
- Исправление или дополнение извлечённых концептов
- Добавление правил поведения

---

### `/semantic vote <id> <up|down>`

**Что делает:** Изменяет уверенность (confidence) концепта.

**Параметры:**
- `<id>` — UUID концепта
- `<up|down>` — повысить или понизить уверенность

**Примеры:**
```bash
/semantic vote a1b2c3d4-e5f6-7890-abcd-ef1234567890 up
/semantic vote a1b2c3d4-e5f6-7890-abcd-ef1234567890 down
```

**Изменение:**
- `up` → confidence + 0.1 (максимум 1.0)
- `down` → confidence - 0.1 (минимум 0.0)

**Пример вывода:**
```
✅ Voted UP on concept: confidence = 0.90
```

**Влияние на систему:**
- ✅ Изменяется поле `confidence`
- ✅ Обновляется поле `updated_at`
- ✅ Концепт сохраняется на диск
- ⚠️ **Критически важно:** влияет на приоритет в поиске

**Практическое применение:**
- `up` — подтвердить правильность концепта
- `down` — отметить ошибочный или неточный концепт
- После нескольких `down` можно удалить концепт

**Стратегия использования:**
```
Низкий confidence (0.3-0.5)  → скорее всего неточный
Средний confidence (0.5-0.7) → требует проверки
Высокий confidence (0.7-0.9) → вероятно правильный
Очень высокий (0.9-1.0)      → проверенный факт
```

---

### `/semantic delete <id>`

**Что делает:** Удаляет концепт из памяти.

**Параметры:**
- `<id>` — UUID концепта

**Примеры:**
```bash
/semantic delete a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

**Пример вывода:**
```
✅ Deleted concept: a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

**Влияние на систему:**
- ✅ Концепт удаляется из HashMap
- ✅ Удаляется из индекса категории
- ✅ Удаляется из файла при следующем сохранении
- ⚠️ **Необратимо** (если не делать backup)

**Когда использовать:**
- Концепт явно неверен
- Дубликат другого концепта
- Устаревшая информация

---

### `/semantic get <id>`

**Что делает:** Показывает полную информацию о концепте.

**Параметры:**
- `<id>` — UUID концепта

**Примеры:**
```bash
/semantic get a1b2c3d4-e5f6-7890-abcd-ef1234567890
```

**Пример вывода:**
```
[a1b2c3d4-e5f6-7890-abcd-ef1234567890] User likes Python (conf: 0.90, used: 42)
   Source: session-20240120-143022
   Created: 2026-01-20 14:30
   Updated: 2026-01-21 10:15
```

**Поля:**
- UUID
- Текст
- Confidence
- Usage count
- Source
- Created/Updated timestamps

**Влияние на систему:** Никакого. Только чтение.

---

### `/semantic merge`

**Что делает:** Объединяет дублирующиеся концепты.

**Как работает:**
1. Находит концепты с одинаковым текстом (без учёта регистра)
2. Объединяет их в один
- Сохраняет максимальный confidence
- Суммирует usage_count
3. Удаляет дубликаты

**Примеры:**
```bash
/semantic merge
```

**Пример вывода:**
```
✅ Merged 3 duplicate concepts
```

**Влияние на систему:**
- ✅ Уменьшает количество концептов
- ✅ Улучшает качество поиска
- ⚠️ Может потерять информацию о разных источниках

**Когда использовать:**
- После массового добавления концептов
- Периодическая очистка (раз в неделю/месяц)
- Перед анализом статистики

---

### `/mem`

**Что делает:** Показывает использование памяти системы.

**Пример вывода:**
```
💻 RAM: 0 MB
🚀 VRAM: 16539 MB
```

**Метрики:**
- **RAM** — системная память (только Linux)
- **VRAM** — память видеокарты (через nvidia-smi)

**Влияние на систему:** Никакого. Только чтение.

---

## Автоматическое Извлечение Концептов

### Как Это Работает

После каждого ответа система автоматически:

1. **Анализ диалога** — LLM (Mistral 7B) анализирует user query + assistant response
2. **Извлечение знаний** — LLM генерирует JSON с концептами
3. **Валидация** — проверка формата JSON
4. **Векторизация** — создание эмбеддингов для каждого концепта
5. **Сохранение** — запись в память и на диск

```
User: Я люблю программировать на Python и использовать VS Code
Assistant: Это отличные инструменты для разработки!

→ Извлечённые концепты:
[
  {"text": "User likes Python", "category": "preferences", "confidence": 0.85},
  {"text": "User uses VS Code", "category": "skills", "confidence": 0.80}
]
```

### Формат Промпта для Извлечения

```xml
<s>[INST] You are a knowledge extraction assistant. Extract facts, preferences, rules, skills from the dialogue below.

Return ONLY a JSON array. Each object must have:
- "text": concise knowledge statement (max 100 chars)
- "category": one of facts, rules, preferences, skills, general
- "confidence": float between 0.0 and 1.0

If no knowledge found, return empty array [].

Dialogue:
User: <query>
Assistant: <response>

Output format: [{"text":"...","category":"...","confidence":0.8}]
NO markdown, NO explanations, NO text before or after. Only JSON.
[/INST]</s>
```

### Ограничения и Особенности

**Ограничения:**
- Максимум ~10 концептов за один диалог
- Текст ограничен 100 символами
- Confidence — субъективная оценка LLM

**Возможные проблемы:**
- Ложные концепты (галлюнации)
- Пропущенные концепты
- Неправильная категоризация

**Решения:**
- Используйте `/semantic vote` для корректировки
- Удаляйте неверные концепты через `/semantic delete`
- Добавляйте пропущенные через `/semantic add`

---

## Влияние на Ответы Системы

### Как Концепты Попадают в Контекст

При обработке запроса система:

1. **Векторизует запрос** → 384-мерный вектор
2. **Ищет в семантической памяти** → топ-10 похожих концептов
3. **Формирует контекст** → KNOWLEDGE секция в промпте
4. **Генерирует ответ** → с учётом найденных концептов

```
Пользователь: На чём мне программировать?

→ Поиск: "программирование"
→ Найдено: "User likes Python" (0.92), "User knows JavaScript" (0.85)

→ Промпт:
[INST] На чём мне программировать? [/INST]

KNOWLEDGE:
[preferences 0.92] User likes Python
[skills 0.85] User knows JavaScript

→ Ответ: Учитывая, что вам нравится Python, рекомендую...
```

### Приоритет Концептов

При формировании KNOWLEDGE секции:

1. **Похожесть** — концепты с высоким similarity первыми
2. **Confidence** — при равной похожести, выше confidence важнее
3. **Категория** — зависит от запроса (preferences важнее для личных вопросов)
4. **Свежесть** — недавние концепты могут быть важнее

### Оптимальное Количество Концептов

| Количество | Эффект |
|------------|--------|
| 0-3 | Мало контекста, ответы могут быть неточными |
| 5-10 | Оптимально для большинства запросов |
| 15+ | Много шума, модель может запутаться |

**Рекомендация:** Используйте `--semantic-top-k 10` (по умолчанию)

---

## Практические Сценарии

### Сценарий 1: Персонализация

**Цель:** Настроить систему под свои предпочтения

```bash
# Добавить предпочтения
/semantic add "Предпочитаю краткие ответы" preferences 0.9
/semantic add "Люблю примеры кода" preferences 0.85
/semantic add "Не использовать смайлики" rules 0.95

# Добавить контекст о себе
/semantic add "Работаю backend-разработчиком" facts 0.9
/semantic add "Знаю Python и Go" skills 0.85
/semantic add "Использую Linux" facts 0.80

# Проверить
/semantic stats
```

**Влияние:** Система будет давать более персонализированные ответы

---

### Сценарий 2: Исправление Ошибок Извлечения

**Проблема:** Система извлекла неверный концепт

```bash
# Найти концепт
/semantic search "пиво"

# Посмотреть детали
/semantic get a1b2c3d4-e5f6-7890-abcd-ef1234567890
# Вывод: [facts] User drinks beer (conf: 0.70)

# Это неверно! Понижаем confidence
/semantic vote a1b2c3d4-e5f6-7890-abcd-ef1234567890 down
# confidence: 0.70 → 0.60

# Если полностью неверно — удаляем
/semantic delete a1b2c3d4-e5f6-7890-abcd-ef1234567890

# Добавляем правильный концепт
/semantic add "Пользователь не пьёт алкоголь" facts 1.0
```

---

### Сценарий 3: Анализ Базы Знаний

**Цель:** Понять, что система знает о пользователе

```bash
# Общая статистика
/semantic stats

# По категориям
/semantic list facts
/semantic list preferences
/semantic list skills

# Поиск по теме
/semantic search "работа"
/emantic search "язык"
/semantic search "любит"
```

---

### Сценарий 4: Очистка Дубликатов

**Проблем:** Накопилось много дубликатов

```bash
# Проверить количество
/semantic stats

# Объединить
/semantic merge

# Проверить результат
/semantic stats
```

---

## Troubleshooting

### Ошибки и Их Решения

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Invalid UUID` | Неправильный формат ID | Используйте UUID из списка |
| `Unknown category` | Опечатка в категории | facts, rules, preferences, skills, general |
| `Concept not found` | Концепт удалён | Проверьте список |
| `JSON parse failed` | Проблема извлечения | Очистите память, добавьте вручную |
| `Out of memory` | Слишком много концептов | Удалите лишние, уменьшите top-k |

### Как Сбросить Память

```bash
# Удалить файл (Linux/macOS)
rm memory_data/semantic_memory.json

# Или очистить через команды
/semantic merge  # объединить дубликаты
# Затем удалить лишние вручную
```

---

## Советы по Эффективному Использованию

1. **Регулярно проверяйте `/semantic stats`** — следите за качеством базы
2. **Используйте vote** — помогайте системе учиться
3. **Группируйте по категориям** — `/semantic list facts` для анализа фактов
4. **Не добавляйте слишком много** — 20-50 концептов достаточно
5. **Периодически мерджите** — раз в неделю `/semantic merge`
6. **Проверяйте источник** — `/semantic get` показывает session_id

---

*ZIGGURAT MIND - Building AI with Memory and Consciousness*
