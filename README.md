# 🏛️ ZIGGURAT MIND

> **AI с гибридной памятью и цифровой персоной**

Система построена на принципе многоуровневого зиккурата, где каждый уровень добавляет новую способность: инфраструктура → память → персона → рассуждение.

## Архитектура

```
┌─────────────────────────────────────────────────────────────────────────┐
│                        🏛️ ZIGGURAT MIND                                 │
├─────────────────────────────────────────────────────────────────────────┤
│  Уровень 3: DEMIURGE (Персона)                                          │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  Archetype System: girlfriend, programmer, scientist, philosopher │   │
│  │  Session Context: предпочтения, история, эмоции                  │   │
│  │  Anti-hallucination: запрет на выдумывание фактов                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────┤
│  Уровень 2: TOTEMS (Гибридная Память)                                   │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  ┌───────────────────┐    ┌───────────────────────────────────┐  │   │
│  │  │   ЭПИЗОДИЧЕСКАЯ   │    │        СЕМАНТИЧЕСКАЯ              │  │   │
│  │  │   (Episodic)      │    │        (Semantic)                 │  │   │
│  │  │                   │    │                                   │  │   │
│  │  │  • Диалоги        │    │  • Факты (facts)                  │  │   │
│  │  │  • Сессии         │    │  • Предпочтения (preferences)     │  │   │
│  │  │  • Контекст       │    │  • Правила (rules)                │  │   │
│  │  │  • Векторный      │    │  • Навыки (skills)                │  │   │
│  │  │    поиск          │    │  • Концепты с confidence          │  │   │
│  │  │                   │    │                                   │  │   │
│  │  │  Vector Store     │    │  Concept Extractor + Deduplication│  │   │
│  │  └───────────────────┘    └───────────────────────────────────┘  │   │
│  └─────────────────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────────────────┤
│  Уровень 1: PRIESTS (Инфраструктура)                                    │
│  Embedding Engine (e5-small, 384-dim) + CUDA/CPU + Persistence          │
├─────────────────────────────────────────────────────────────────────────┤
│  Уровень 0: LOGOS (Генерация)                                           │
│  Mistral 7B Inference + Tokenization + Sampling                         │
└─────────────────────────────────────────────────────────────────────────┘
```

## Гибридная Система Памяти

### Эпизодическая Память (Episodic)

Хранит полную историю диалогов с векторным поиском по схожести.

**Расположение:** `memory_data/episodic/`
- `sessions.json` - история диалогов
- `embeddings.bin` - векторные представления

**Активация:** `--enable-memory`

### Семантическая Память (Semantic)

Извлекает и хранит структурированные знания о пользователе.

**Расположение:** `memory_data/semantic/`
- `semantic_memory.json` - концепты с категориями

**Категории концептов:**

| Категория | Описание | Пример |
|-----------|----------|--------|
| `facts` | Факты о пользователе | "Работает программистом" |
| `preferences` | Предпочтения | "Любит пиццу" |
| `rules` | Правила/ограничения | "Не любит ранние подъемы" |
| `skills` | Навыки | "Знает Python" |
| `general` | Общие знания | "Интересуется AI" |

**Активация:** `--enable-semantic`

### Особенности Семантической Памяти

1. **Извлечение только из реплик пользователя** - не из ответов ассистента
2. **Детекция саморяскрытий** - только "я люблю", "мой любимый", "предпочитаю"
3. **Дедупликация** - при similarity > 0.92 пропускается дубликат
4. **Обнаружение противоречий** - "люблю пиццу" vs "не люблю пиццу"
5. **Автоматическая коррекция JSON** - исправление кривого вывода модели

## Персонная Система (Demiurge)

### Архетипы

| Архетип | Имя | Описание |
|---------|-----|----------|
| `girlfriend` | Алиса | Заботливая подруга-разработчица |
| `programmer` | Код | Технический ассистент |
| `scientist` | Эйнштейн | Научный подход |
| `philosopher` | Сократ | Философские вопросы |

### Команды Персоны

```bash
--archetype girlfriend    # Выбрать архетип
/persona                  # Управление персоной
/context                  # Показать контекст сессии
```

## Data Flow

```
┌──────────────────────────────────────────────────────────────────────┐
│                         User Input                                    │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    Semantic Memory Search                             │
│         (ищет предпочтения/факты о пользователе)                      │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    Episodic Memory Search                             │
│           (ищет похожие диалоги при запросах о прошлом)               │
│   Активируется только при: "помнишь?", "что я говорил?", etc.        │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    Persona + Context Build                            │
│         (форматирование промпта с учётом персоны)                     │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    Mistral 7B Generation                              │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    Concept Extraction                                  │
│         (извлекает саморяскрытия из реплики пользователя)             │
│         ТОЛЬКО: "я люблю", "мой", "предпочитаю", "нравится"          │
└──────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────┐
│                    Deduplication + Storage                            │
│         (дедупликация, противоречия, сохранение в JSON)               │
└──────────────────────────────────────────────────────────────────────┘
```

## Команды

### Сборка

```bash
# GPU (CUDA) - рекомендуется
cargo build --features cuda --bin ziggurat-unified

# CPU-only
cargo build --bin ziggurat-unified
```

### Запуск

```bash
# Полный режим с гибридной памятью
cargo run --features cuda -- --interactive --archetype girlfriend --enable-memory --enable-semantic

# Только эпизодическая память
cargo run --features cuda -- --interactive --archetype girlfriend --enable-memory

# Одиночный запрос
cargo run --features cuda -- --prompt "я люблю пиццу" --enable-semantic

# Тихий режим (без DEBUG)
cargo run --features cuda -- --interactive --archetype girlfriend --enable-memory --enable-semantic --quiet
```

### Параметры

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--prompt TEXT` | Запрос для обработки | - |
| `--interactive` | Интерактивный режим | false |
| `--archetype NAME` | Архетип персоны | "programmer" |
| `--enable-memory` | Эпизодическая память | false |
| `--enable-semantic` | Семантическая память | false |
| `--memory-top-k N` | Похожих диалогов | 5 |
| `--semantic-top-k N` | Концептов | 10 |
| `--quiet` | Тихий режим | false |
| `--cpu` | CPU вместо GPU | false |

### Интерактивный режим

```bash
cargo run --features cuda -- --interactive --archetype girlfriend --enable-memory --enable-semantic --quiet
```

Команды в чате:
```
привет                          # Приветствие
я люблю пиццу                   # Сохранится в память
что я люблю?                    # Запрос из памяти
/semantic list                  # Показать концепты
/semantic stats                 # Статистика памяти
/context                        # Контекст сессии
quit                            # Выход с сохранением
```

## Структура Файлов

```
memory_data/
├── context/                    # Контекст сессии
│   └── {archetype}_context.json
├── episodic/                   # Эпизодическая память
│   ├── sessions.json
│   └── embeddings.bin
└── semantic/                   # Семантическая память
    └── semantic_memory.json
```

## Тестирование

### Полный тест памяти

```cmd
rem Очистка памяти
rmdir /s /q memory_data\semantic 2>nul

rem Запуск
cargo run --features cuda -- --interactive --archetype girlfriend --enable-memory --enable-semantic --quiet
```

Ввести в чате:
```
привет
я люблю пиццу
нет я люблю суши
что я люблю?
quit
```

**Ожидаемый результат:**
- "я люблю пиццу" → сохранено
- "нет я люблю суши" → сохранено как "I love sushi" (не "I don't love sushi")
- "что я люблю?" → "You love пиццу and суши"

### Проверка файла памяти

```cmd
type memory_data\semantic\semantic_memory.json
```

Пример содержимого:
```json
{
  "total_concepts": 2,
  "concepts": [
    {"text": "I love pizza", "category": "preferences", "confidence": 1.0},
    {"text": "I love sushi", "category": "preferences", "confidence": 0.9}
  ]
}
```

### Тест одной командой

```cmd
echo "я люблю программировать" | cargo run --features cuda -- --enable-semantic
```

## Компоненты

| Компонент | Путь | Описание |
|-----------|------|----------|
| **Priests** | `src/priests/` | Embedding engine, device management |
| **Totems** | `src/totems/` | Memory systems (episodic, semantic, vector) |
| **Demiurge** | `src/demiurge/` | Persona system, archetypes, evolution |
| **Logos** | `src/logos/` | Mistral 7B inference |

### Ключевые файлы

- `src/main_unified.rs` - Единая точка входа
- `src/priests/embeddings.rs` - Embedding engine (e5-small)
- `src/totems/semantic/manager.rs` - Семантическая память + deduplication
- `src/totems/episodic/mod.rs` - Dialogue memory manager
- `src/demiurge/persona.rs` - Persona system
- `src/demiurge/archetype.rs` - Archetype definitions

## Анти-Галлюцинационные Меры

1. **Извлечение только из реплик пользователя** - модель не может выдумать факты из своего ответа
2. **Детекция саморяскрытий** - только явные утверждения ("я люблю", "мой")
3. **Запрет в промпте** - "Не придумывай и не упоминай детали прошлых разговоров, которых не было"
4. **Дедупликация** - одинаковые факты не накапливаются
5. **Обнаружение противоречий** - при конфликте обновляется существующий факт

## Модели

- **Embedding**: `intfloat/multilingual-e5-small` (384-мерные векторы)
- **LLM**: `mistralai/Mistral-7B-Instruct-v0.2`

Расположение: `models/embeddings/` и `models/mistral-7b-instruct/`

## Требования

- NVIDIA GPU с CUDA (опционально)
- Rust 1.70+
- CMake (для candle)

---

## 📋 TODO LIST (Кор Философия)

> **Принцип**: Минимальный жизнеспособный продукт с последующей эволюцией. Каждая задача должна быть конкретной и измеримой.

### 🔴 ПРИОРИТЕТ: КРИТИЧЕСКИЙ

- [ ] **Исправить сохранение памяти при --prompt режиме**
  - Сейчас: семантическая память не сохраняется при одном запросе
  - Нужно: добавить save после обработки
  - Файл: `src/main_unified.rs:~1350`

- [ ] **Добавить обработку ошибок экстракции**
  - Сейчас: модель может вернуть невалидный JSON
  - Нужно: fallback на регулярные выражения
  - Файл: `src/main_unified.rs:ConceptExtractorImpl`

### 🟠 ПРИОРИТЕТ: ВЫСОКИЙ

- [ ] **Улучшить детекцию противоречий**
  - Сейчас: только "люблю" vs "не люблю"
  - Нужно: расширить на "нравится", "предпочитаю", "ненавижу"
  - Файл: `src/totems/semantic/manager.rs:is_contradiction`

- [ ] **Добавить нормализацию текста перед similarity**
  - Сейчас: "I love pizza" != "i love pizza"
  - Нужно: lowercase + стемминг
  - Файл: `src/totems/semantic/manager.rs:add_concept`

- [ ] **Увеличить порог similarity для дедупликации**
  - Сейчас: 0.92
  - Проблем: "I love pizza" и "I love pizza with pineapple" считаются дубликатами
  - Нужно: 0.95 или разные пороги для разных категорий

### 🟡 ПРИОРИТЕТ: СРЕДНИЙ

- [ ] **Добавить команду /semantic merge**
  - Ручное объединение похожих концептов
  - Файл: `src/main_unified.rs:handle_semantic_command`

- [ ] **Добавить голосование по концептам**
  - Пользователь может подтвердить/отклонить концепт
  - Влияет на confidence

- [ ] **Добавить автоматическую очистку старых концептов**
  - Концепты с confidence < 0.3 и usage_count = 0
  - Период: 30 дней

- [ ] **Улучшить промпт экстракции для английского**
  - Сейчас: фокус на русском
  - Нужно: поддержка "I prefer", "my favorite is"

### 🟢 ПРИОРИТЕТ: НИЗКИЙ

- [ ] **Добавить тесты для semantic memory**
  - Unit tests для deduplication
  - Integration tests для extraction

- [ ] **Добавить команду /memory stats**
  - Общая статистика: концептов, категории, использование

- [ ] **Улучшить DEBUG вывод**
  - Краткий режим: только результаты
  - Подробный: весь процесс

- [ ] **Добавить поддержку других языков**
  - English, Deutsch, etc.
  - Файл: `src/main_unified.rs:ConceptExtractorImpl::extract`

- [ ] **Оптимизировать производительность**
  - Кэширование эмбеддингов
  - Batch extraction

### 📊 МЕТРИКИ ДЛЯ ИЗМЕРЕНИЯ

| Метрика | Целевое значение | Текущее |
|---------|-----------------|---------|
| Memory precision | > 95% | ? |
| Extraction accuracy | > 90% | ? |
| Deduplication rate | > 80% | ? |
| Contradiction detection | > 85% | ? |
| Memory persistence | 100% | ? |

---

**ZIGGURAT MIND - Building AI with Memory and Consciousness 🏛️**
