# 🏛️ ZIGGURAT MIND

> **AI с долговременной памятью и сознанием**

Система построена на принципе многоуровневого зиккурата, где каждый уровень добавляет новую способность.

## Архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                    🏛️ ZIGGURAT MIND                              │
├─────────────────────────────────────────────────────────────────┤
│  Уровень 3: LOGOS (Рассуждение)                                  │
│  Mistral 7B Inference + Tokenization + Sampling                  │
├─────────────────────────────────────────────────────────────────┤
│  Уровень 2: TOTEMS (Память)                                      │
│  ┌─────────────────┬─────────────────┬──────────────────┐        │
│  │ Эпизодическая   │ Семантическая   │ Векторное        │        │
│  │ память          │ память          │ хранилище        │        │
│  │ (диалоги)       │ (концепты)      │ (cosine search)  │        │
│  └─────────────────┴─────────────────┴──────────────────┘        │
├─────────────────────────────────────────────────────────────────┤
│  Уровень 1: PRIESTS (Инфраструктура)                             │
│  Embedding Engine (e5-small) + Device Management + Resources     │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow

```
User Input
    │
    ├──────────────────────────────────────────────────────┐
    │                                                      │
    ▼                                                      ▼
┌───────────────┐                              ┌───────────────────┐
│ Embedding     │                              │ Current Session   │
│ Engine        │──────┐                       │ Context           │
│ (e5-small)    │      │                       │ (last 5 turns)    │
└───────────────┘      │                       └───────────────────┘
                       │                                │
                       ▼                                │
                ┌──────────────┐                        │
                │ Vector Store │◄───────────────────────┘
                │ (similarity) │        Get relevant past dialogues
                └──────────────┘
                       │
                       ▼
                ┌──────────────────┐
                │ Keyword Search   │  ← Fallback для улучшения recall
                │ (fallback)       │
                └──────────────────┘
                       │
                       ▼
                ┌──────────────────┐
                │ Merge & Dedupe   │  ← Удаление дубликатов
                └──────────────────┘
                       │
                       ▼
                ┌──────────────────┐
                │ Enhanced Prompt  │
                │ Builder          │
                │ + Memory Label   │  ← "MEMORY (use this to answer...)"
                └──────────────────┘
                       │
                       ▼
                ┌──────────────┐
                │ Mistral 7B   │
                │ (generate)   │
                └──────────────┘
                       │
                       ▼
                ┌──────────────┐
                │ Response +   │
                │ Save to      │
                │ Memory       │
                └──────────────┘
```

## Компоненты

| Компонент | Путь | Описание |
|-----------|------|----------|
| **Priests** | `src/priests/` | Embedding engine, device management |
| **Totems** | `src/totems/` | Memory systems (episodic, semantic, vector) |
| **Logos** | `src/logos/` | Mistral 7B inference |

### Ключевые файлы

- `src/main_unified.rs` - Единая точка входа
- `src/priests/embeddings.rs` - Embedding engine (e5-small)
- `src/totems/episodic/mod.rs` - Dialogue memory manager
- `src/totems/episodic/persistence.rs` - Memory persistence
- `src/totems/retrieval/vector_store.rs` - Vector search
- `src/logos/mod.rs` - Mistral 7B inference

## Быстрый старт

```bash
# Сборка с CUDA
cargo build --features cuda --bin ziggurat-unified

# Одиночный запрос
cargo run --bin ziggurat-unified --features cuda -- \
  --prompt "Расскажи о себе" \
  --enable-memory

# Интерактивный режим с памятью
cargo run --bin ziggurat-unified --features cuda -- \
  --enable-memory \
  --interactive
```

## Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `--prompt TEXT` | Запрос для обработки | - |
| `--enable-memory` | Включить эпизодическую память | false |
| `--memory-top-k N` | Количество похожих диалогов | 5 |
| `--persona NAME` | Имя личности ассистента | "assistant" |
| `--interactive` | Интерактивный режим | false |
| `--cpu` | Использовать CPU вместо GPU | false |
| `--temperature 0.7` | Температура генерации | 0.7 |
| `--sample-len N` | Максимум токенов | 2048 |

## Примеры использований

### Одиночный запрос
```bash
cargo run --bin ziggurat-unified --features cuda -- \
  --prompt "Привет, как дела?" \
  --enable-memory
```

### Интерактивный режим
```bash
cargo run --bin ziggurat-unified --features cuda -- \
  --enable-memory \
  --interactive
```

В интерактивном режиме:
```
📝 You: Привет
📝 You: Меня зовут Мария
📝 You: Кто я?
📝 You: quit
```

## Память и Персистентность

Данные сохраняются в директорию `memory_data/`:

```
memory_data/
├── sessions.json      # Текст диалогов (JSON)
├── embeddings.bin     # Векторные представления (binary)
└── metadata.json      # Метаданные хранилища
```

### Особенности текущей реализации

- Эмбеддинги (384-мерные векторы) сохраняются в бинарный файл `embeddings.bin`
- Формат файла: Header (32 bytes) + Index entries (32 bytes each) + Embedding data (4 bytes per float)
- При запуске система автоматически загружает сохранённые эмбеддинги
- Текущая сессия и история сессий восстанавливаются из `sessions.json`
- Hybrid search: semantic similarity + keyword fallback для улучшения recall
- Восстановление метаданных (user_query, assistant_response) из sessions.json

## Тестирование памяти

Система протестирована на сохранение и восстановление долговременной памяти.

### Пример работы (29.01.2026)

```bash
# Запуск системы
cargo run --bin ziggurat-unified --features cuda -- --enable-memory --interactive

📝 You: i love lamborghini car
[Система отвечает и сохраняет в память]

💾 Memory: 1 turns in current session
DEBUG: Saved 2 embeddings (3.00 KB) to "memory_data/embeddings.bin"

# Перезапуск системы (Ctrl+C, затем снова запустить)
📝 You: which car i love?

🤖 Assistant:
DEBUG: Restored 2 embeddings to vector store
DEBUG: Found 1 similar dialogues
DEBUG: Similar[0] = [Similarity: 0.894] User: i love lamborghini car...

Based on your previous statement, you love Lamborghini cars. Lamborghini is known for producing high-performance sports cars...
```

### Результат тестирования

| Тест | Статус |
|------|--------|
| Сохранение сессии на диск | ✅ Работает |
| Сохранение эмбеддингов в бинарный файл | ✅ Работает |
| Восстановление памяти после перезапуска | ✅ Работает |
| Semantic similarity поиск | ✅ Работает (0.894) |
| Keyword fallback поиск | ✅ Работает |
| Mistral использует контекст памяти | ✅ Работает |

### Метрики памяти

| Метрика | Значение |
|---------|----------|
| Embedding dimension | 384 (e5-small) |
| Top-K результатов | 5 |
| Similarity threshold | ~0.8+ для релевантных воспоминаний |
| Восстановление памяти | Мгновенное (бинарный файл) |
| Размер файла эмбеддингов | ~1.5 KB на диалог |

### Алгоритм поиска памяти

1. **Semantic Search** - cosine similarity между запросом и эмбеддингами (приоритет)
2. **Keyword Search** - дополнительный поиск по ключевым словам для улучшения recall
3. **Deduplication** - исключение дубликатов по session_id + turn
4. **Контекст сессии** - добавление последних 5 обменов в промпт

## Требования

- NVIDIA GPU с CUDA (опционально)
- Rust 1.70+
- CMake (для сборки candle)

## Зависимости

```toml
[dependencies]
candle-core = "0.9"
candle-nn = "0.9"
candle-transformers = "0.9"
mistral-pure = "0.1"
tokenizers = "0.21"
chrono = "0.4"
uuid = "1"
serde = "1.0"
anyhow = "1.0"
clap = "4.0"
```

## Модели

Система использует:
- **Embedding**: `intfloat/multilingual-e5-small` (384-мерные векторы)
- **LLM**: `mistralai/Mistral-7B-Instruct-v0.2`

Модели должны быть размещены в директории `models/`:
```
models/
├── embeddings/
│   ├── tokenizer.json
│   ├── config.json
│   └── model.safetensors
└── mistral-7b-instruct/
    ├── tokenizer.json
    ├── config.json
    └── model.safetensors.index.json
```

## Инструкция для Windows

### Предварительные требования

1. **Установите Rust**
   ```powershell
   winget install Rustlang.Rust.MSVC
   ```

2. **Установите CMake**
   ```powershell
   winget install Kitware.CMake
   ```

3. **Установите NVIDIA CUDA Toolkit** (для GPU)
   - Скачайте с https://developer.nvidia.com/cuda-downloads
   - Или используйте winget: `winget install NVIDIA.CUDA Toolkit`

### Сборка

Откройте **PowerShell** от имени администратора:

```powershell
# Перейдите в директорию проекта
cd C:\Users\zikkuratti\Documents\agent\zikkurat-mind

# Сборка с поддержкой CUDA (рекомендуется для GPU)
cargo build --features cuda --bin ziggurat-unified

# Или сборка для CPU (если нет GPU)
cargo build --bin ziggurat-unified
```

### Запуск

#### 1. Одиночный запрос

```powershell
# Перейдите в директорию проекта
cd C:\Users\zikkuratti\Documents\agent\zikkurat-mind

# Запустите с запросом
.\target\debug\ziggurat-unified.exe --prompt "Привет, как дела?" --enable-memory

# Или используйте cargo run
cargo run --bin ziggurat-unified --features cuda -- --prompt "Твой любимый цвет?" --enable-memory
```

#### 2. Интерактивный режим

```powershell
cargo run --bin ziggurat-unified --features cuda -- --enable-memory --interactive
```

После запуска вводите запросы:
```
📝 You: Привет
📝 You: Меня зовут Иван
📝 You: Как меня зовут?
📝 You: quit
```

#### 3. С параметрами

```powershell
# Изменить количество похожих диалогов
cargo run --bin ziggurat-unified --features cuda -- `
  --prompt "Расскажи о себе" `
  --enable-memory `
  --memory-top-k 5

# Изменить температуру генерации
cargo run --bin ziggurat-unified --features cuda -- `
  --prompt "Напиши стихотворение" `
  --temperature 0.8

# CPU режим (без CUDA)
cargo run --bin ziggurat-unified -- `
  --prompt "Привет" `
  --cpu `
  --enable-memory
```

### Очистка памяти

Если возникают ошибки загрузки, удалите старые файлы памяти:

```powershell
# PowerShell
Remove-Item -Path "memory_data\sessions.json", "memory_data\embeddings.bin", "memory_data\metadata.json" -ErrorAction SilentlyContinue

# CMD
del memory_data\sessions.json memory_data\embeddings.bin memory_data\metadata.json
```

### Просмотр DEBUG логов

```powershell
# PowerShell - фильтр по ключевым словам
cargo run --bin ziggurat-unified --features cuda -- --prompt "Тест" 2>&1 | Select-String "DEBUG"

# CMD
cargo run --bin ziggurat-unified --features cuda -- --prompt "Тест" 2>&1 | findstr "DEBUG"
```

### Частые проблемы

| Проблема | Решение |
|----------|---------|
| Ошибка CUDA out of memory | Используйте `--cpu` или уменьшите `--sample-len` |
| Модель не найдена | Проверьте наличие файлов в `models/mistral-7b-instruct/` |
| Ошибка deserialization | Удалите файлы в `memory_data/` |
| Cargo не найден | Добавьте Rust в PATH или перезапустите терминал |

### Скрипт для быстрого запуска ( PowerShell )

Создайте файл `run.ps1`:

```powershell
# run.ps1
param(
    [string]$Prompt = "Привет!",
    [switch]$Interactive = $false,
    [switch]$Cpu = $false,
    [int]$MemoryTopK = 3
)

$features = if ($Cpu) { "" } else { "--features cuda" }
$memory = if ($Interactive) { "--enable-memory --interactive" } else { "--enable-memory" }

$cmd = "cargo run --bin ziggurat-unified $features -- --prompt '$Prompt' --memory-top-k $MemoryTopK $memory"

Invoke-Expression $cmd
```

Запуск:
```powershell
.\run.ps1 -Prompt "Как дела?" -MemoryTopK 5
.\run.ps1 -Interactive
.\run.ps1 -Cpu -Interactive
```

### Расположение файлов после сборки

```
C:\Users\zikkuratti\Documents\agent\zikkurat-mind\
├── target/
│   ├── debug/
│   │   └── ziggurat-unified.exe  # Исполняемый файл
│   └── release/
│       └── ziggurat-unified.exe  # Оптимизированная версия
├── memory_data/                   # Файлы памяти
├── models/                        # Модели
└── src/                           # Исходный код
```

---

**ZIGGURAT MIND - Building AI with Memory and Consciousness 🏛️**
